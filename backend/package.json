{
  "name": "backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node --env-file=../.env --watch server.js",
    "start": "node --env-file=../.env server.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.91.1",
    "dotenv": "^17.4.2",
    "express": "^4.21.2",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
