{
  "name": "brielles-loop",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "build": "npm run build --workspace frontend",
    "security:socket:reachable-runtime": "node scripts/check-socket-reachable-runtime.mjs"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "esbuild": "^0.28.0",
    "react-router-dom": "^7.14.2",
    "zustand": "^5.0.12"
  }
}
